# Athena Pi - Caddy Configuration
# Place this file at /etc/caddy/Caddyfile on your Raspberry Pi

athena.local {
    # Reverse proxy to FastAPI
    reverse_proxy localhost:8000
    
    # Enable compression
    encode gzip
    
    # Security headers
    header {
        X-Content-Type-Options nosniff
        X-Frame-Options DENY
        X-XSS-Protection "1; mode=block"
    }
}

# Optional: HTTPS with Tailscale (auto-generated certs)
# If using Tailscale, Caddy can get certs automatically
*.ts.net {
    tls {
        dns tailscale
    }
    reverse_proxy localhost:8000
}
